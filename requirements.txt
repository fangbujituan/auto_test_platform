# ============================================================================
# 自动化测试平台依赖
# ----------------------------------------------------------------------------
# 支持的 Python 版本: >=3.11, <3.14
#   - 下限 3.11：受 deepagents 限制（其 PyPI wheel 仅发布 >=3.11）
#   - 上限 3.13：3.14 与 SQLAlchemy 2.0.x 的 FastIntFlag 实现不兼容
#                （SQLAlchemy issue #12298），且部分二进制包尚未提供 3.14 wheel
#   - 推荐版本：3.13
#
# 版本锁定策略
#   1. 业务稳定库（Flask / SQLAlchemy / pandas 等）——只标 ">=" 下限
#      这些库 API 稳定、彼此独立，留出补丁版本升级空间
#   2. LangChain 生态（langchain / langchain-core / langgraph / deepagents 等）
#      ——完全不锁版本
#      原因：这些包之间是紧耦合关系，相互的版本依赖范围很窄。任何一个写死
#      其它包就会冲突。让 pip 整体解析最稳。
# ============================================================================

# ============================================================================
# Web 层（Flask 生态）
# ============================================================================
flask>=3.0.0
flask-sqlalchemy>=3.1.0
flask-cors>=4.0.0
flask-smorest>=0.44.0
marshmallow>=3.20.0

# ============================================================================
# 数据库 / ORM
# ============================================================================
# 注意：SQLAlchemy 必须 >=2.0.36，避免 Python 3.13+ 的兼容问题
sqlalchemy>=2.0.36
pymysql>=1.1.0

# ============================================================================
# 工具与基础设施
# ============================================================================
requests>=2.31.0
pyyaml>=6.0
cryptography>=42.0.0
python-dotenv>=1.0.0
pandas>=2.2.0
openpyxl>=3.1.0

# ============================================================================
# 调度与定时任务
# ============================================================================
APScheduler>=3.10.0
croniter>=1.4.0

# ============================================================================
# WSGI 服务器（生产环境部署）
# ============================================================================
gunicorn>=21.0.0

# ============================================================================
# 测试
# ============================================================================
pytest>=8.0.0

# ============================================================================
# AI / LangChain 生态（不锁版本，由 pip 整体解析）
# ----------------------------------------------------------------------------
# 这些包相互之间依赖范围很窄（例如 langchain 1.3.x 要求 langchain-core 1.4.x），
# 一旦锁死某个就会冲突。统一交给 pip 解析。
# ============================================================================
langchain[openai]
langchain-deepseek
langchain-mcp-adapters
langgraph
langgraph-cli[inmem]
deepagents

# ============================================================================
# UI 自动化与其它
# ============================================================================
beautifulsoup4
loguru
fastapi
playwright
websockets
